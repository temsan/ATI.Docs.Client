
Процедура КоманднаяПанельСохранить(Кнопка)
	
	СтруктураСвойств = Новый Структура;
	СтруктураСвойств.Вставить("comment", Комментарий);
	СтруктураСвойств.Вставить("ogrn", ОГРН);
	СтруктураСвойств.Вставить("ifns", ИФНС);
	СтруктураСвойств.Вставить("region_code", КодРегиона);
	
	Закрыть(СтруктураСвойств);
	
КонецПроцедуры

//служебная функция для автотеста
Процедура ВыполнитьКоманду(ТекстКоманды) Экспорт
	Выполнить(ТекстКоманды);
КонецПроцедуры

Процедура КоманднаяПанельЗакрыть(Кнопка)
	Закрыть(Неопределено);
КонецПроцедуры
